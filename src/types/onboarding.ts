export interface OnboardingDepartment {
  id: string;
  name: string;
  description?: string;
  organization_id: string;
}

export interface OnboardingFunctionItem {
  id: string;
  name: string;
  description?: string;
}